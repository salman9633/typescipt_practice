 enum position{
    forward,
    mid,
    defence,
    goalkeeper
 }

 let messi= position.forward


const enum positions{
    forward=5,//if we assign an number to enum variable the next variables number will be increment
    mid,//6 will be the vale here
    defence,//7 on here
}

let xavi=positions.mid
console.log(xavi);//output will be 6
